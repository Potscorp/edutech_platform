# 🏗️ COMPLETE ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  index.html  │  │  admin.html  │  │ player.html  │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                │
│                            │                                    │
│  ┌─────────────────────────▼─────────────────────────┐        │
│  │           JavaScript Files (Connected)            │        │
│  ├───────────────────────────────────────────────────┤        │
│  │  • config.js    → API Configuration               │        │
│  │  • script.js    → Main Logic (✅ API Connected)   │        │
│  │  • admin.js     → Admin Logic (✅ API Connected)  │        │
│  │  • player.js    → Course Player                   │        │
│  │  • payment.js   → Payment Processing              │        │
│  └───────────────────────┬───────────────────────────┘        │
│                          │                                      │
└──────────────────────────┼──────────────────────────────────────┘
                           │
                           │ HTTP Requests (fetch API)
                           │ with JWT Tokens
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                      BACKEND LAYER                              │
│                   Django REST Framework                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              API Endpoints (Port 8000)                  │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │                                                         │  │
│  │  Authentication APIs (accounts/views.py)               │  │
│  │  ├─ POST /api/auth/signup/     ✅ Connected           │  │
│  │  ├─ POST /api/auth/login/      ✅ Connected           │  │
│  │  └─ GET  /api/auth/user/       ✅ Connected           │  │
│  │                                                         │  │
│  │  Course APIs (courses/views.py)                        │  │
│  │  ├─ GET    /api/courses/           ✅ Connected       │  │
│  │  ├─ GET    /api/courses/<id>/      ✅ Connected       │  │
│  │  ├─ POST   /api/courses/create/    ✅ Connected       │  │
│  │  ├─ PUT    /api/courses/<id>/update/ ✅ Connected     │  │
│  │  └─ DELETE /api/courses/<id>/delete/ ✅ Connected     │  │
│  │                                                         │  │
│  │  Payment APIs (courses/views.py)                       │  │
│  │  ├─ GET  /api/courses/payments/    ✅ Connected       │  │
│  │  └─ POST /api/courses/payment/process/ ✅ Connected   │  │
│  │                                                         │  │
│  └─────────────────────┬───────────────────────────────────┘  │
│                        │                                       │
│  ┌─────────────────────▼───────────────────────────────────┐  │
│  │              Business Logic Layer                       │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • accounts/models.py  → User Model                    │  │
│  │  • courses/models.py   → Course Model                  │  │
│  │  • courses/payments.py → Payment Model                 │  │
│  │  • JWT Authentication  → Token Generation              │  │
│  │  • bcrypt             → Password Hashing               │  │
│  └─────────────────────┬───────────────────────────────────┘  │
│                        │                                       │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         │ PyMongo Driver
                         │ MongoDB Connection String
                         │
┌────────────────────────▼───────────────────────────────────────┐
│                     DATABASE LAYER                             │
│                   MongoDB Atlas (Cloud)                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Database: edulearn_db                                        │
│  Cluster: cluster0.zbira1f.mongodb.net                        │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  Collections:                                        │    │
│  │                                                      │    │
│  │  📁 users                                            │    │
│  │     ├─ _id (ObjectId)                               │    │
│  │     ├─ name, email, password (hashed)               │    │
│  │     ├─ role (student/instructor/admin)              │    │
│  │     ├─ status, enrolled_courses                     │    │
│  │     └─ created_at, updated_at                       │    │
│  │                                                      │    │
│  │  📁 courses                                          │    │
│  │     ├─ _id (ObjectId)                               │    │
│  │     ├─ title, description, category                 │    │
│  │     ├─ price, instructor_id                         │    │
│  │     ├─ modules[], students[]                        │    │
│  │     ├─ status, thumbnail                            │    │
│  │     └─ created_at, updated_at                       │    │
│  │                                                      │    │
│  │  📁 payments                                         │    │
│  │     ├─ _id (ObjectId)                               │    │
│  │     ├─ user_id, course_id                           │    │
│  │     ├─ amount, payment_method                       │    │
│  │     ├─ status, transaction_id                       │    │
│  │     └─ created_at                                   │    │
│  │                                                      │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
└────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════
                        DATA FLOW EXAMPLE
═══════════════════════════════════════════════════════════════════

User Creates Course:
─────────────────────

1. User fills form in admin.html
2. JavaScript calls handleCreateCourse()
3. Function calls API.createCourse(courseData)
4. config.js sends POST to http://localhost:8000/api/courses/create/
5. Django receives request in courses/views.py → create_course()
6. Course.create_course() in models.py processes data
7. PyMongo inserts document into MongoDB courses collection
8. MongoDB returns inserted document with _id
9. Django sends response back to frontend
10. Frontend updates UI and shows success message

═══════════════════════════════════════════════════════════════════
                      AUTHENTICATION FLOW
═══════════════════════════════════════════════════════════════════

User Login:
───────────

1. User enters email/password in login form
2. handleLogin() calls API.login(credentials)
3. POST to /api/auth/login/
4. Django validates credentials with bcrypt
5. If valid, generates JWT token with user info
6. Token sent back to frontend
7. Frontend stores token in localStorage
8. All subsequent API calls include token in Authorization header
9. Django validates token before processing requests

═══════════════════════════════════════════════════════════════════
                        TECHNOLOGY STACK
═══════════════════════════════════════════════════════════════════

Frontend:
  • HTML5, CSS3, JavaScript (Vanilla)
  • Font Awesome Icons
  • Fetch API for HTTP requests
  • localStorage for token storage

Backend:
  • Python 3.12
  • Django 4.2.7
  • Django REST Framework 3.14.0
  • PyJWT 2.8.0 (JWT tokens)
  • bcrypt 4.1.2 (password hashing)
  • django-cors-headers 4.3.1

Database:
  • MongoDB Atlas (Cloud)
  • PyMongo 4.6.1 (driver)
  • Collections: users, courses, payments

═══════════════════════════════════════════════════════════════════
                         STATUS: ✅ LIVE
═══════════════════════════════════════════════════════════════════

All layers connected and working!
Frontend ↔️ Backend ↔️ Database = FULLY INTEGRATED
```
